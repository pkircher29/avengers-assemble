# Chuck + Claude prototype

Authorized by Paul: two participants only, Chuck leads. No automatic team activation.

## Model and reasoning control
Paul explicitly delegates model and reasoning-level selection to Chuck for every participant, including Chuck. Make model and reasoning/effort explicit per assignment, chosen for task complexity, capability, latency, and cost. Show requested and verified effective runtime settings in the mission record/status. Verify each runtime's supported values rather than assuming effort labels are equivalent across providers. Record changes with rationale. Use supported switching mechanisms; never claim Chuck's active model changed merely because a config file or another session changed. If a runtime requires user interaction or restart, report the limitation and preserve the mission handoff. If Chuck cannot change his own active model live, send Paul a Telegram request identifying the desired model, supported reasoning level, and concise rationale. Continue working with the current model while awaiting Paul; do not block the mission just for a model upgrade. Verify the actual model after Paul switches it. Discover and verify the approved Telegram destination and sending path before sending; report delivery failures honestly and avoid duplicate requests. This authorization covers model-change notifications, not unrelated messages. Do not auto-fallback to a different model silently or treat this delegation as approval for new subscriptions or unbounded spending.

1. Chuck creates a local standard-library runner. It opens a visible Windows console, consumes an explicit file inbox, streams actual Claude Code text/tool events, and preserves response/session evidence. No network listener or scheduled service. One request at a time, bounded turns/budget/time. Claude is restricted to file tools in its workspace; no shell, external MCP, or delegates.
2. Chuck asks Claude a design question; Claude responds with a question. Chuck answers by resuming the exact session and assigns a bounded artifact: message validation and tests. Claude alone edits its worker directory. Chuck alone edits runner infrastructure.
3. Chuck runs generated tests and verifies actual message exchange/session continuity, visible window existence, output artifacts and worker shutdown. Record limitations: streamed print-mode console, not interactive Claude TUI; local file-based transport, not full dashboard or general peer bus.
4. Create /avengers-assemble skill with explicit user activation, Chuck leadership, readiness gates, and the proven prototype commands. No broader team starts automatically.

Acceptance: actual Claude response and acknowledged follow-up in same session; code artifact independently tested; visible console verified; status/logs survive worker exit; no unrelated project/config changes.
