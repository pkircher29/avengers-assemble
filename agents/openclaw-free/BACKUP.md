# Backup — OpenClaw free-model worker

Status: blocked; no private profile-scoped repository or verified remote commit exists.

Required scope: role Soul, non-secret OpenClaw profile config, allowed skills, owned knowledge graph, daily notes, durable memory, sessions/transcripts, and task/handoff state.

Exclusions: `.env`, credentials, OAuth/API tokens, private keys, certificate material, caches, installers, build output, and disposable runtime artifacts.

Completion requires: credential-redacted snapshot integrity check, private remote push, exact remote SHA verification, and recorded timestamp/exclusions/redactions/failure state.

Do not describe this worker as operational until this document names its private remote and last verified SHA.
